--- 
- hosts: all
  become: yes

  tasks:
  - name: Install the package "apache2"
    apt:
     name: apache2
     state: present

  - name: Start service apache2, on node server
    service:
      name: "apache2"
      state: started  